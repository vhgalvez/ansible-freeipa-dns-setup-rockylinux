version: '3.8'

services:
  coredns:
    image: coredns/coredns:latest
    restart: unless-stopped
    ports:
      - "1053:53/udp"
      - "1053:53/tcp"
    volumes:
      - ./Corefile:/etc/coredns/Corefile
    environment:
      - TZ=UTC
    networks:
      - coredns-net

networks:
  coredns-net:
    driver: bridge