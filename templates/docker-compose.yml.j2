version: '3.7'
services:
  coredns:
    image: coredns/coredns:latest
    container_name: coredns
    volumes:
      - /etc/coredns/Corefile:/etc/coredns/Corefile
      - /etc/hosts:/etc/hosts
    ports:
      - "53:53/tcp"
      - "53:53/udp"
    restart: always