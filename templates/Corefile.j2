. {
    hosts {
        {% for record in dns_records %}
        {{ record.ip }} {{ record.name }}.{{ domain_name }} {{ record.name }}
        {% endfor %}
        10.17.3.12 jenkins.cefaslocalserver.com jenkins
        10.17.3.13 jenkins.cefaslocalserver.com jenkins
        fallthrough
    }

    forward . 8.8.8.8 8.8.4.4 {
        max_concurrent 1000
    }

    cache 30
    loop
    reload 5s
    loadbalance
    health {
        lameduck 5
    }

    prometheus :9153
    log
    errors
}